# SKILLS.md

## Local preview
Preview the site locally before pushing:
```
cd docs
python -m http.server 8000
```
(On this Windows machine, `python3` is not a recognized alias — use `python`.)
Visit `http://localhost:8000`.
